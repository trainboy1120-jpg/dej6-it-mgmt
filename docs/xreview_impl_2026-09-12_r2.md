# xreview report (2026-09-12 03:28)

**Final: FAIL**

**Stopped: manual fix mode (--fixer none): findings remain; fix them by hand and re-run for re-review** (human review needed)

Spec: 対象は DEJ6 IT機器管理アプリ index.html(単一ファイル・Bootstrap5・Supabase REST 直叩き・GitHub Pages 配信)への『機種別 修理手順ガイド v2.1』の実装。docs/impl_repair_guide.patch が今回の全差分(index.html.bak → index.html、追加のみ・既存機能の削除なし)、index.html は差分適用後の全文(文脈確認用)、tests/test_repair_guide.py が静的テスト(171 PASS: 1行1操作・step_extra とフローの整合・first_aid≥3・URL https・識別子の一意定義(新規22件が各1回、旧名0)・inline onclick に動的値なし・全 href が esc(guideSafeUrl())・モーダル数7・見出しA〜E順・LEGACY/PIN 保全・ディープリンクは loadAll().finally 後)、tests/test_repair_guide_e2e.py が Playwright 動的テスト(81 PASS: Supabase/Firebase は route で差し替え。ナビ→9タブ→カード数=フロー長、ディープリンク ?guide= 空/不正/正常 かつ loadAll 後に登録済み住所が summary に反映、デバイス→手順→「いまここ」1個、編集者状態を localStorage に入れてメモ入力→ガイド表示中は statusModal を隠し shown.bs.modal 後に Esc で閉じると同機器・同メモ値で復帰、XSS 住所がテキスト表示、javascript: URL→#、URLコピー形式、印刷ウィンドウの title/見出し/ステップ数/.btn 非表示/A4≤3枚、モバイル390px)。設計プランは docs/repair_guide_plan.md(cross-model review r14 PASS 済)。前回 round1(FAIL 2 major)への対応: ①起動を loadAll().finally(() => ディープリンクなら guideOpen) に変更し、loadAll 内でも guideModal 表示中なら guideRender で再描画(5分毎自動更新時の住所反映) ②e2e ③で編集者状態を add_init_script で投入し #mMemoInput='memo-keep' の保持を必須アサート。加えて guideOpen は Modal.getOrCreateInstance に変更(リスナー重複回避)。テストは出力先がパイプでも落ちないよう stdout を UTF-8 に reconfigure、run_all は子に PYTHONUTF8=1 を渡す。③の Esc 後は固定待ちでなく statusModal の shown.bs.modal を待つ(5秒で FAIL)。さらに「statusModal のフェードイン中に手順ボタンを押すと Bootstrap が hide() を無視して何も起きない」不具合を実測(e2e が不安定化した原因)→ guideStatusFading(show/shown イベントで追跡)中の押下は shown.bs.modal 後にやり直す実装を追加し、e2e ③' で速いタップでもガイドが開くことを検証。レビュー観点: (1)差分が既存機能(ステータス更新・一括操作・Manifest・履歴・設定・SOP・sync との契約)を壊していないか (2)描画の XSS 境界(動的値は esc()、URL は guideSafeUrl、機種値はホワイトリスト) (3)多重モーダルの扱い(hide→hidden.bs.modal で開く→閉じたら statusModal を再表示)に競合・取りこぼしがないか (4)ディープリンクと起動順序 (5)テストが仕様を実際に検証しているか(形だけの PASS がないか) (6)現場で誤解を生む表現。blocking = blocker または major。仕様どおりで指摘対象外: ガイド本文はコード内定数(DB化・編集UIは対象外)/写真・動画は対象外/未検証Wiki下位URLは載せない(QuickLinkトップのみ)/SB_KEY(anon key)と PIN_HASH がコード内にあるのは既存設計で GitHub Pages 公開前提(PIN 平文は無い)/静的文字列のみの onclick(guideOpen(typeFilter==='ALL'?'SSP':typeFilter)・guideCopyUrl()・guidePrint()・guideOpenFromStatus())は既存アプリの作法に合わせて許容/他モーダルが new bootstrap.Modal を使うのは既存コードで対象外/print は別ウィンドウ document.write 方式(既存 printManifest と同じ)/Avery は FLOWS 上「SIM起票済み」を通過するのがツールの制約で『ツール上の表示』と明示して受容/Other は FLOWS に無いため GUIDE_DEFAULT_FLOW を使う設計(openStatus の既定配列も同定数に置換済)/1行1操作の判定は check_one_action.py の規則を正とし通過した行の分割要求は対象外/first_aid と cautions は説明文で規則の対象外/テストは file:// で index.html を開くため Pages 固有の挙動(相対パス等)は対象外/e2e の clipboard 実書き込みは file:// では権限が取れないため guideDeepLink() の戻り値検証で代替(受容)/e2e が Bootstrap の公開イベント shown.bs.modal を待つのは実利用(フェード完了後に操作)と同じで受容/本文の事実は確認済みで真偽は対象外。

## Round 1: reviewer=openai.gpt-5.6-sol verdict=FAIL
ステータスモーダルを同一ページで再度開いた場合、ガイド遷移が停止する重大なモーダル管理不整合があります。E2E は各機器を新規ページで検証しているため、この再利用シナリオを検出できません。
- [major] index.html:1506 2回目以降に開いた statusModal から修理手順ガイドを開けない可能性がある
  - evidence: guideOpenFromStatus は `const el = document.getElementById('statusModal'), sm = bootstrap.Modal.getInstance(el)` で登録済みインスタンスを取得して `sm.hide()` しますが、openStatus は982行目で毎回 `new bootstrap.Modal(document.getElementById('statusModal')).show()` を実行しています。Bootstrap は同一要素の新しいインスタンスを既存登録へ置換しないため、再度開いたモーダルの表示状態と `getInstance()` が返す旧インスタンスの `_isShown` が一致せず、旧インスタンスの `hide()` が何もせず `hidden.bs.modal` も発火しない場合があります。E2E も107〜137行目で機器ごとに新規ページを作るため、同一ページでの再オープンを検証していません。
  - fix: openStatus の表示処理も `bootstrap.Modal.getOrCreateInstance(document.getElementById('statusModal')).show()` に統一してください。さらに同一ページで statusModal を開く→閉じる→別機器で再度開く→修理手順ボタンを押す、というE2Eを追加し、statusModal が隠れて guideModal が表示されることを検証してください。
