# xreview report (2026-09-12 03:32)

**Final: PASS**

Spec: 対象は DEJ6 IT機器管理アプリ index.html(単一ファイル・Bootstrap5・Supabase REST 直叩き・GitHub Pages 配信)への『機種別 修理手順ガイド v2.1』の実装。docs/impl_repair_guide.patch が今回の全差分(index.html.bak → index.html、追加のみ・既存機能の削除なし)、index.html は差分適用後の全文(文脈確認用)、tests/test_repair_guide.py が静的テスト(172 PASS: 1行1操作・step_extra とフローの整合・first_aid≥3・URL https・識別子の一意定義(新規22件が各1回、旧名0)・inline onclick に動的値なし・全 href が esc(guideSafeUrl())・モーダル数7・見出しA〜E順・LEGACY/PIN 保全・ディープリンクは loadAll().finally 後)、tests/test_repair_guide_e2e.py が Playwright 動的テスト(89 PASS: Supabase/Firebase は route で差し替え。ナビ→9タブ→カード数=フロー長、ディープリンク ?guide= 空/不正/正常 かつ loadAll 後に登録済み住所が summary に反映、デバイス→手順→「いまここ」1個、編集者状態を localStorage に入れてメモ入力→ガイド表示中は statusModal を隠し shown.bs.modal 後に Esc で閉じると同機器・同メモ値で復帰、XSS 住所がテキスト表示、javascript: URL→#、URLコピー形式、印刷ウィンドウの title/見出し/ステップ数/.btn 非表示/A4≤3枚、モバイル390px)。設計プランは docs/repair_guide_plan.md(cross-model review r14 PASS 済)。前回 round2(FAIL 1 major: openStatus の new bootstrap.Modal と getInstance の不一致懸念)への対応: openStatus も Modal.getOrCreateInstance に統一し、静的テストで statusModal/guideModal に new bootstrap.Modal が無いことを確認、e2e ③'' で同一ページ内「機器A→閉じる→機器B→手順→Esc→復帰→もう一度手順→Esc→復帰」の2周を検証(shown.bs.modal カウントで確認)。round1(FAIL 2 major)への対応: ①起動を loadAll().finally(() => ディープリンクなら guideOpen) に変更し、loadAll 内でも guideModal 表示中なら guideRender で再描画(5分毎自動更新時の住所反映) ②e2e ③で編集者状態を add_init_script で投入し #mMemoInput='memo-keep' の保持を必須アサート。加えて guideOpen は Modal.getOrCreateInstance に変更(リスナー重複回避)。テストは出力先がパイプでも落ちないよう stdout を UTF-8 に reconfigure、run_all は子に PYTHONUTF8=1 を渡す。③の Esc 後は固定待ちでなく statusModal の shown.bs.modal を待つ(5秒で FAIL)。さらに「statusModal のフェードイン中に手順ボタンを押すと Bootstrap が hide() を無視して何も起きない」不具合を実測(e2e が不安定化した原因)→ guideStatusFading(show/shown イベントで追跡)中の押下は shown.bs.modal 後にやり直す実装を追加し、e2e ③' で速いタップでもガイドが開くことを検証。レビュー観点: (1)差分が既存機能(ステータス更新・一括操作・Manifest・履歴・設定・SOP・sync との契約)を壊していないか (2)描画の XSS 境界(動的値は esc()、URL は guideSafeUrl、機種値はホワイトリスト) (3)多重モーダルの扱い(hide→hidden.bs.modal で開く→閉じたら statusModal を再表示)に競合・取りこぼしがないか (4)ディープリンクと起動順序 (5)テストが仕様を実際に検証しているか(形だけの PASS がないか) (6)現場で誤解を生む表現。blocking = blocker または major。仕様どおりで指摘対象外: ガイド本文はコード内定数(DB化・編集UIは対象外)/写真・動画は対象外/未検証Wiki下位URLは載せない(QuickLinkトップのみ)/SB_KEY(anon key)と PIN_HASH がコード内にあるのは既存設計で GitHub Pages 公開前提(PIN 平文は無い)/静的文字列のみの onclick(guideOpen(typeFilter==='ALL'?'SSP':typeFilter)・guideCopyUrl()・guidePrint()・guideOpenFromStatus())は既存アプリの作法に合わせて許容/statusModal/guideModal 以外のモーダル(login/bulk/history/config/sop)が new bootstrap.Modal を使うのは既存コードでガイドと相互作用しないため対象外/print は別ウィンドウ document.write 方式(既存 printManifest と同じ)/Avery は FLOWS 上「SIM起票済み」を通過するのがツールの制約で『ツール上の表示』と明示して受容/Other は FLOWS に無いため GUIDE_DEFAULT_FLOW を使う設計(openStatus の既定配列も同定数に置換済)/1行1操作の判定は check_one_action.py の規則を正とし通過した行の分割要求は対象外/first_aid と cautions は説明文で規則の対象外/テストは file:// で index.html を開くため Pages 固有の挙動(相対パス等)は対象外/e2e の clipboard 実書き込みは file:// では権限が取れないため guideDeepLink() の戻り値検証で代替(受容)/e2e が Bootstrap の公開イベント shown.bs.modal を待つのは実利用(フェード完了後に操作)と同じで受容/本文の事実は確認済みで真偽は対象外。

## Round 1: reviewer=openai.gpt-5.6-sol verdict=PASS
前回指摘の Bootstrap Modal インスタンス不一致は、statusModal と guideModal の双方で getOrCreateInstance を使う形に修正されています。ディープリンクの起動順序、入力値を保持したモーダル復帰、フェード中の高速操作、XSS・URL境界、既存フローとの整合をコードとテストで確認でき、blocker/major に該当する問題は見つかりませんでした。

### verify command rc=0
```
[test_repair_guide.py] rc=0 passes=172  | ALL PASS | summary: PASS (checks run: see above) 
[test_repair_guide_e2e.py] rc=0 passes=89  | summary: PASS (0 fail)
```
